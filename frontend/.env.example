VITE_API_URL=http://127.0.0.1:4000/api

# Google Cloud OAuth 2.0 Web Client ID. This is safe to expose to the browser.
VITE_GOOGLE_CLIENT_ID=

# In Cloud Run these can be provided as runtime env vars; frontend/docker-entrypoint.sh
# writes them into /env.js when the container starts.
